import 'package:flutter/material.dart';

class DiteScreen extends StatelessWidget {
  const DiteScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
